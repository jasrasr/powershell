# 💬 ChatGPT in your PowerShell Terminal Window!

Yes, you can — and here’s how.

## 🧠 Setup Instructions

1. **Add this function to your `$PROFILE`.**  
   💡 *Better yet:* create a `$commonProfilePath` in your `$PROFILE` so you have **one file to edit** across all PowerShell sessions and terminals — PS5, PS7, VS Code, Admin, etc.

2. **Create this file:**  
   `invoke-chatgpt.ps1`

3. **Make sure to edit or point to an existing folder:**  
   ```powershell
   $githubpath = "C:\Users\<you>\OneDrive - Middough\Documents\GitHub"

4. Set the environment variable for your OpenAI API key (keep this secret!).

   - 🔑 Get your API key here: [https://platform.openai.com/api-keys](https://platform.openai.com/api-keys)  
   - Set it in PowerShell:

     ```powershell
     setx OPENAI_API_KEY "sk-yourkeyhere"
     ```

5. Reload your PowerShell session so it recognizes the new `OPENAI_API_KEY` environment variable.

6. Run a test command:

   ```powershell
   chatgpt "tell me a joke"
